machine:
  xcode:
    version: "8.0"
  environment:
    GYM_CODE_SIGNING_IDENTITY: 'iPhone Distribution: Axilis d.o.o. (5FZ9X78DWN)'

#dependencies:
#  pre:
#   - npm install -g react-native-cli

deployment:
  beta_distribution:
    branch: master
    commands:
      - gym